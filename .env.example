# MongoDB Atlas connection string. MONGO_URI is also accepted as an alias.
MONGODB_URI=mongodb+srv://username:password@cluster.example.mongodb.net/dexlabz

# Generate a random value of at least 32 characters.
SESSION_SECRET=replace-with-a-long-random-secret

NEXT_PUBLIC_APP_URL=http://localhost:3000
